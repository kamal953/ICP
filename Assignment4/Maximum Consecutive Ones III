class Solution {
    public int longestOnes(int[] nums, int k) {
        int left=0;
        int ans=0;
        int count=0;
        for(int right=0;right<nums.length;right++){
            if(nums[right]==0){
                count++;
            }
            while(count>k){
                if(nums[left]==0){
                    count--;
                }
                left++;
            }

            ans = Math.max(ans,right-left+1);
        }

        return ans;
    }
}
// Time Complexity: O(n)
// Each element is visited at most twice—once by the right pointer and once by the left—so the overall time is linear in the length of the array.

// Space Complexity: O(1)
// You're using only a few integer variables (left, right, count, ans), so space usage is constant.
